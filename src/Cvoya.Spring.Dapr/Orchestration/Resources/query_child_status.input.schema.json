{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "query_child_status.input",
  "description": "Input for query_child_status — cheap status check for a child without a full inspect.",
  "type": "object",
  "properties": {
    "address": {
      "type": "string",
      "description": "Canonical scheme:no-dash-hex address of a direct child."
    }
  },
  "required": ["address"],
  "additionalProperties": false
}
