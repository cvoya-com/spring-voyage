{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "list_members.input",
  "description": "Input for the list_members orchestration tool — enumerates the caller's own direct members (empty array for leaf agents). Takes no arguments.",
  "type": "object",
  "properties": {},
  "additionalProperties": false
}
