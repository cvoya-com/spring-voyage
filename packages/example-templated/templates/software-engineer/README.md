# software-engineer (AgentTemplate)

Archetype for a generalist software engineer. Use as the target of a concrete `kind: Agent` with `from: software-engineer`. The instance inherits `ai:`, `role:`, `capabilities:`, `instructions:`, and `expertise:` by default; declare any of those fields on the instance to override.

This template does **not activate** — it has no platform identity until instantiated.
