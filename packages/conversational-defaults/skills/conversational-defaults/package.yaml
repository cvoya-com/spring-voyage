apiVersion: spring.voyage/v1
kind: Skill
name: conversational-defaults
description: sv.conversational.defaults — the default conversational bundle. Relies on the platform-provided response contract and tool catalog, and grants the `memory` category on top.
