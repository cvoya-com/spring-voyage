apiVersion: spring.voyage/v1
kind: Agent
name: ada
description: Ada — software engineer specialised in numerical / algorithmic work. Concrete instance of the `software-engineer` template.
from: software-engineer
# Inherits ai, role, capabilities, instructions, and expertise from the
# software-engineer template. This instance overrides the description
# only; everything else flows through from the template.
